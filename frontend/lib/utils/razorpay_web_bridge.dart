// lib/utils/razorpay_web_bridge.dart

void openRazorpayWeb({
  required String key,
  required int amount,
  required String title,
  required String contact,
  required String email,
  required Function onSuccess, // Ensure this matches the Impl exactly
}) {
  // This stays empty. It's just a placeholder for Android.
}